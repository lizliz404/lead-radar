# lead-radar — SEO / GSC / GA4 / GEO audit (2026-08-07)

- Live: https://lead-radar.lizliz.xyz/
- Repo: `/home/ubuntu/projects/lead-radar`
- Stack: Next.js 16 App Router landing (`web/`) → static `out/` → Cloudflare Pages
- Evidence: source audit + live `curl` (2026-08-07). Fixes in source only. **No commit / no push.**

## Live evidence (pre-fix)

| Probe | Result |
|---|---|
| `GET /` | HTTP 200, no `x-robots-tag`; `html lang="en"` |
| `GET /robots.txt` | HTTP 200, `Allow: /`, Sitemap → `https://lead-radar.lizliz.xyz/sitemap.xml` |
| `GET /sitemap.xml` | HTTP 200, valid urlset (home, `/examples`, `/preview`, 3 example slugs); `lastmod` stuck at 2026-05-09 (stale deploy) |
| `GET /og-image.png` | HTTP 200 (1200×630 in source) |
| `GET /favicon.svg` / `favicon.ico` / `favicon-32.png` / `apple-touch-icon.png` | **HTTP 404** on live (present in `web/public/` @ HEAD / origin) |
| GA4 in live HTML | **Absent** (`G-TXVLTJJ878` / `gtag` not in live `<head>`) — present in source `layout.tsx` since `5ef6ba2` |
| Icons / theme-color / application-name in live HTML | Absent (stale deploy vs source metadata) |
| JSON-LD live | `SoftwareApplication` + `FAQPage` present, but SoftApp missing `url` / `image` / `author` / `isPartOf` (pre-`ca2a7a0` payload) |
| GSC meta / DNS TXT | Absent (not invented) |
| Title / description / canonical / OG / Twitter | Present on live; description was 220 chars (over 160) |

## Checklist

| Item | Status | Action taken | file:line |
|---|---|---|---|
| **GSC** sitemap.xml exists, valid, real URLs | **ok** | Next `app/sitemap.ts` already emits real absolute URLs | `web/app/sitemap.ts:6-35` |
| **GSC** robots.txt allows pages, references sitemap | **ok** | Next `app/robots.ts` Allow `/` + Sitemap | `web/app/robots.ts:5-14` |
| **GSC** verification token | **residual** | None in source meta or DNS TXT; did not invent | — |
| **GSC** single canonical, no http/www duplicates | **ok** | `metadataBase` + per-page `alternates.canonical`; live canonical `https://lead-radar.lizliz.xyz` | `web/app/layout.tsx:41` |
| **GSC** no noindex on indexable pages | **ok** | `robots.index/follow` true; no `x-robots-tag` on `/` | `web/app/layout.tsx:66-76` |
| **GA4** gtag present with `G-TXVLTJJ878` | **ok (source) / live stale** | Left intact in source; live missing until Hermes redeploy | `web/app/layout.tsx:89-98` |
| **GA4** ID intact (house rule) | **ok** | Unchanged `G-TXVLTJJ878` | `web/app/layout.tsx:90,96` |
| **GA4** head async placement | **ok** | Async gtag in `<head>` of root layout (all routes) | `web/app/layout.tsx:90` |
| **GEO** entity signals in visible copy | **fixed** | Footer now states maker (Liz) + category (Reddit demand research preview) | `web/app/page.tsx:369-374` |
| **GEO** JSON-LD WebSite / SoftwareApplication / FAQ | **fixed** | Added truthful `WebSite` + publisher Person; SoftApp + FAQPage already present | `web/app/page.tsx:116-145` |
| **GEO** specific factual copy | **ok** | Method steps, signal taxonomy, $49/$199 offers, validation loop statuses already specific | `web/app/page.tsx:5-62,181-298` |
| **GEO** FAQ / How-it-works / About | **ok** | Visible FAQ + Method sections; FAQPage matches visible Q&A | `web/app/page.tsx:321-336` |
| **Base** `<title>` unique ~60 | **ok / tightened** | Home default 50 chars; examples title shortened so template stays ≤60 | `web/app/layout.tsx:9-11`, `web/app/examples/page.tsx:6` |
| **Base** meta description 120–160 pain-first | **fixed** | 145 chars, pain-first rewrite | `web/app/layout.tsx:12-13` |
| **Base** OG + Twitter cards + 1200×630 image | **ok** | Tags + `/og-image.png` (1200×630); live OG image 200 | `web/app/layout.tsx:42-65`, `web/public/og-image.png` |
| **Base** hreflang / lang | **ok (N/A i18n)** | `html lang="en"`; EN-only site | `web/app/layout.tsx:81` |
| **Base** one h1, logical h2 | **ok** | Single home h1; section h2s; examples have own h1 | `web/app/page.tsx:155`, live HTML |
| **Base** favicon + apple-touch + theme-color | **fixed (source) / live stale** | Icons already in `public/` + metadata; moved `themeColor` to Next 16 `viewport` export | `web/app/layout.tsx:6-8,33-40`, `web/public/*` |
| **Base** JSON-LD syntactically valid | **ok / extended** | Absolute URLs; WebSite + SoftApp + FAQPage; no invented AggregateRating | `web/app/page.tsx:84-145` |

## Changes made

1. `web/app/layout.tsx` — pain-first meta description (145); `viewport.themeColor`; removed deprecated metadata `themeColor`; GA4 untouched
2. `web/app/page.tsx` — `WebSite` JSON-LD; footer entity line (Liz / category)
3. `web/app/examples/page.tsx` — shorter title for template budget
4. `docs/audit-2026-08-07-seo.md` — this file

## Verification

- Build/typecheck **skipped**: no `web/node_modules`; did not touch `next.config.ts` / build plumbing — metadata, JSON-LD, and copy only
- Live still reflects pre-`ca2a7a0` / pre-`5ef6ba2` HTML until Hermes commit+push+Pages rebuild

## Residual

| Priority | Item | Notes |
|---|---|---|
| **P0** | Redeploy Pages from current `main` | Live missing favicons, GA4, enriched SoftApp JSON-LD, theme-color — all already in git HEAD (`ca2a7a0`, `5ef6ba2`) plus this pass |
| **P1** | GSC property verification | Add meta or DNS TXT when Liz owns the token; then submit sitemap |
| **P1** | Sitemap `lastmod` refresh | Will update on next static build (`new Date()` in `sitemap.ts`) |
| **P2** | Example slug titles + template | Some `"… Simulated Report Format \| Lead Radar"` strings may exceed ~60 chars |
| **P2** | Organization JSON-LD | Skipped — Person publisher is more truthful than inventing an Organization |
| **P2** | `@next/third-parties` GoogleAnalytics | Dependency present but unused; raw gtag in layout is fine and house-ID intact |
