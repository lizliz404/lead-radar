# lead-radar — SEO / GSC / GA4 / GEO audit (2026-08-08)

- Live: https://lead-radar.lizliz.xyz/
- Repo: `/home/ubuntu/projects/lead-radar`
- Stack: Next.js 16 App Router static export (`web/` → `out/`) → Cloudflare Pages
- Canonical host (binding/product URL): `https://lead-radar.lizliz.xyz`
- Evidence: live `curl` 2026-08-08 + source review. **No commit / no push.**

## Verdict

**FIX** — source SEO mostly ready (incl. 2026-08-07 pass on `origin/main`); live Pages still serves a stale build. This pass adds remaining P1 GEO/CWV/on-page polish in source.

## Live evidence (2026-08-08)

| Probe | Result |
|---|---|
| `GET /` | HTTP 200; `html lang="en"`; no `x-robots-tag` noindex on home |
| `http → https` | 301 → `https://lead-radar.lizliz.xyz/` |
| `GET /robots.txt` | 200; `Allow: /`; `Sitemap: https://lead-radar.lizliz.xyz/sitemap.xml` |
| `GET /sitemap.xml` | 200; **6** `<loc>` (home, `/examples`, `/preview`, 3 example slugs); `lastmod` stuck at `2026-05-09` |
| GA4 `G-TXVLTJJ878` in live HTML | **0** (absent) |
| theme-color / application-name / favicon link tags | Absent on live |
| `GET /favicon.ico` `/favicon.svg` `/apple-touch-icon.png` | **404** |
| `GET /og-image.png` | 200 (1200×630 in source) |
| `GET /llms.txt` | **404** (added in source this pass) |
| Live meta description | 220 chars (stale); source is 145 |
| Live JSON-LD | `SoftwareApplication` + `FAQPage` only; SoftApp missing `url`/`image`/`author`/`isPartOf`; no `WebSite` |
| GSC verification meta | Absent (expected: covered by **lizliz.xyz domain property** — not a per-subdomain token gap) |
| `_redirects` splat loop | None in repo |

## Checklist

| Item | Status | Action | file:line |
|---|---|---|---|
| Technical robots.txt Allow + Sitemap | **ok** | Live + source align | `web/app/robots.ts:5-14` |
| Technical sitemap.xml real absolute `<loc>` | **ok (source) / live stale lastmod** | 6 locs; `lastModified: now` on rebuild | `web/app/sitemap.ts:6-35` |
| Technical single canonical host | **ok** | `metadataBase` + `alternates.canonical`; live canonical `https://lead-radar.lizliz.xyz` | `web/app/layout.tsx:11,44` |
| Technical http→https | **ok** | Live 301 | — |
| Technical noindex leaks on money pages | **ok** | `robots.index/follow` true | `web/app/layout.tsx:69-79` |
| GA4 `G-TXVLTJJ878` head async | **ok (source) / live missing** | Intact ×3 in layout; do not change ID | `web/app/layout.tsx:92-99` |
| GSC verification token | **n/a (domain property)** | Subdomain under `lizliz.xyz` domain property; do not invent meta | — |
| GEO entity signals visible | **ok** | Footer: Built by Liz · Reddit demand research preview | `web/app/page.tsx:373-379` |
| GEO JSON-LD WebSite / SoftApp / FAQ | **ok (source) / live stale** | WebSite + SoftApp + FAQPage; SoftApp `isPartOf` → lead-radar host | `web/app/page.tsx:84-145` |
| GEO llms.txt | **fixed (source)** | Added root `llms.txt` | `web/public/llms.txt` |
| GEO AI crawler CF managed blocks | **residual (flag)** | Code robots Allow `/`; CF managed AI scrapers may still block GPTBot/ClaudeBot — operator check | — |
| On-page title ~60 | **ok / fixed slugs** | Home 50; examples index OK; slug titles now absolute ≤56 | `web/app/layout.tsx:12-15`, `web/app/examples/[slug]/page.tsx:20` |
| On-page description 120–160 | **ok / fixed** | Home 145; preview 142; slug descs 140–154 (was using 189–215 summaries) | `web/app/layout.tsx:16-17`, `web/app/preview/page.tsx:6-7`, `web/app/examples/[slug]/page.tsx:21` |
| On-page OG/Twitter + 1200×630 | **ok** | Tags + `/og-image.png` live 200 | `web/app/layout.tsx:45-68` |
| On-page one H1 | **ok** | Single home H1 | `web/app/page.tsx:171` |
| On-page favicon + apple-touch + theme-color | **ok (source) / live 404** | Icons in `web/public/`; `viewport.themeColor` | `web/app/layout.tsx:6-8,36-42` |
| CWV hero dims / LCP hint | **fixed (source)** | `width`/`height` + `fetchPriority="high"` on hero | `web/app/page.tsx:186-192` |
| Structured data truthfulness | **ok** | Offers $49/$199 match visible Access; FAQ matches visible Q&A; no fake AggregateRating / price:0 | `web/app/page.tsx:22-62,84-145` |
| Content E-E-A-T / anti-slop | **ok** | Pain-first copy; preview stage disclosed; samples labeled simulated | home + examples |

## Changes made (this pass)

1. `web/public/llms.txt` — GEO summary + product/access/maker facts
2. `web/app/page.tsx` — SoftApp `isPartOf` → `lead-radar.lizliz.xyz`; hero `width`/`height`/`fetchPriority`
3. `web/app/examples/[slug]/page.tsx` — absolute titles ≤56; meta descriptions 140–154
4. `web/app/preview/page.tsx` — pain-first description 142 chars
5. `docs/audit-2026-08-08-seo.md` — this file

Unchanged by design: GA4 ID; brand assets/icons/fonts/palette; pre-existing dirty Python files (`config.example.yaml`, `lead_radar/*`, `tests/*`).

## Verification

- `grep -c G-TXVLTJJ878 web/app/layout.tsx` → **3** (intact; layout not edited)
- Build/typecheck **skipped**: no `web/node_modules`; metadata/copy/JSON-LD/`llms.txt` only — no build plumbing change
- Live still stale until Pages rebuild from current `main` (+ this dirty SEO tree after Hermes commit)

## Residual

| Priority | Item | Notes |
|---|---|---|
| **P0** | Cloudflare Pages redeploy from current source | Live missing GA4, favicons, theme-color, WebSite JSON-LD, shortened desc, enriched SoftApp, `llms.txt`. `main` already had 08-07 SEO commits (`626cd39` et al.) at `origin` sync — **build root / Pages hook likely broken or not rebuilding**; confirm Pages root = `web`, build = `npm run build`, output = `out` |
| **P1** | Confirm CF managed robots AI-scraper policy | Flag only; not fixable from repo `robots.ts` if CF dashboard blocks GPTBot/ClaudeBot |
| **P1** | Submit sitemap in GSC after live catches up | Domain property covers host; submit `https://lead-radar.lizliz.xyz/sitemap.xml` |
| **P2** | Hero PNG weight ~707KB | Dims set; compression/format swap is asset work — out of brand-change scope this pass |
| **P2** | Organization JSON-LD | Person publisher/author is more truthful than inventing an org |
| **P2** | Inline SVG `<title>Lead Radar logo</title>` | Extra title nodes in HTML body; a11y-valid, not document title |
